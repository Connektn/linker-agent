apiVersion: v1
kind: Service
metadata:
  name: {{ include "connektn-gateway.fullname" . }}
  labels:
    {{- include "connektn-gateway.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "connektn-gateway.selectorLabels" . | nindent 4 }}
